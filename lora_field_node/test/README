## CLI Commands

- `version` — показує версію firmware, Git hash і dirty status
- `log` — показує кільцевий лог
- `config get` — показує поточну конфігурацію
- `config set <key> <value>` — змінює параметр конфігурації
- `config reset` — скидає конфіг до значень за замовчуванням